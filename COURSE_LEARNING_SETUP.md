# إعداد مسار التعلم والشهادات في QuadraLevel

هذه الميزة تضيف Dashboard خاصة بكل كورس، وقفلًا تسلسليًا للفصول، واختبارات تُصحح على الخادم، ومشروع تخرج يُقيّمه الذكاء الاصطناعي، وشهادة رقمية برمز تحقق فريد.

## قاعدة البيانات

شغّل ملف `course-learning-seed.sql` مرة واحدة في Supabase SQL Editor بعد أن تكون جداول Marketplace الأساسية موجودة. الملف idempotent؛ تشغيله مرة أخرى يحدّث تعريفات الفصول والاختبارات والمشروع ولا يحذف محاولات أو مستخدمين أو مدفوعات.

المتوقع بعد التشغيل هو 18 درسًا، و18 اختبارًا، ومشروع تخرج واحد لكل من `marketing-launch` و`marketing-growth`.

## إعداد الذكاء الاصطناعي

يستخدم Backend نفس إعداد Mentor الحالي من خلال `OPENAI_API_KEY` الموجود في بيئة Render. لا تضع المفتاح في GitHub أو الواجهة الأمامية، ولا ترسله في المحادثة. النموذج الافتراضي هو `gpt-5-mini` ويمكن ضبطه من خلال `OPENAI_MODEL` في Render إذا لزم الأمر.

إذا لم يكن `OPENAI_API_KEY` موجودًا في Render، ستظل الاختبارات والقفل يعملان، لكن تقديم مشروع التخرج سيعرض رسالة تفيد بأن التقييم الآلي غير مضبوط حتى تُضاف قيمة المفتاح من لوحة Render.

## مسار المستخدم

يفتح زر التعلم `dashboard.html?course=marketing-launch` أو `dashboard.html?course=marketing-growth`. يعرض Dashboard الكورس الفصول التسعة، ويفتح الفصل الأول فقط في البداية. بعد اجتياز اختبار الفصل الأول يفتح الفصل الثاني، وهكذا.

المحتوى نفسه يبقى خلف Content Gateway المحمي. لذلك لا يكفي تغيير الرابط يدويًا للوصول إلى فصل مقفول؛ التحقق موجود في Backend أيضًا.

بعد اجتياز جميع الاختبارات يفتح مشروع التخرج. يرسل الطالب النص إلى Backend، ويقيّمه الذكاء الاصطناعي بدرجة من 100 وفق معايير الفهم والتطبيق والاستراتيجية والوضوح. درجة النجاح الافتراضية 70%. يستطيع الطالب إعادة التقديم إذا لم ينجح.

لا تصدر الشهادة إلا بعد اجتياز الاختبارات والمشروع، وشراء الكورس. التجربة تسمح بالتعلم والتقييم، لكن الشهادة مرتبطة بالوصول المدفوع حتى يظل نموذج البيع الحالي كما هو.

## التحقق من الشهادة

كل شهادة تحصل على `certificate_number` و`verification_code` مختلفين. صفحة التحقق العامة هي `/verify-certificate`، ويمكن مشاركة الرابط الموجود في صفحة الشهادة مع أي جهة.
